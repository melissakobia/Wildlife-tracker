{{#partial "content"}}
    <div>
        <div class="row">
            <div class="col-md-6" style="background-image: url('/images/elephant2.jpg'); width: 28rem; background-position: center; background-repeat: no-repeat; background-size: cover;">
                <div class="container endangered-heading">
                    <h1>Non-Endangered Animals</h1>

                </div>


            </div>
            <div class="col-md-6">
                <div class="container">

                </div>
                {{#if nonEndangeredAnimals}}
                    {{#each nonEndangeredAnimals}}
                        <div class="card">
                            <div class="card-body">
                                <p class="card-title">Animal Name: {{name}}</p>
                                <hr>
                                <p>Age: {{age}}</p>

                            </div>
                        </div>

                    {{/each}}
                {{else}}
                    <h1>No Non-Endangered Animals!!</h1>
                {{/if}}

            </div>

        </div>

    </div>
{{/partial}}

{{> layout.hbs}}